<Window x:Class="EasySave.WPF.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="{Binding L[app.title]}" Height="600" Width="900"
        MinHeight="400" MinWidth="600"
        WindowStartupLocation="CenterScreen">

	<Window.Resources>
		<BooleanToVisibilityConverter x:Key="BoolToVis"/>
	</Window.Resources>

	<Grid Margin="16">
		<Grid.RowDefinitions>
			<RowDefinition Height="Auto"/>
			<RowDefinition Height="*"/>
			<RowDefinition Height="Auto"/>
		</Grid.RowDefinitions>

		<ToolBar Grid.Row="0" Margin="0,0,0,8">
			<Button Command="{Binding StartAllCommand}"
                    Content="{Binding L[btn.run.all]}" Padding="8,4"/>
			<Separator/>
			<Button Command="{Binding AddJobCommand}"
                    Content="{Binding L[btn.add]}" Padding="8,4"/>
			<Button Command="{Binding EditJobCommand}"
                    Content="{Binding L[btn.edit]}" Padding="8,4"/>
			<Button Command="{Binding DeleteJobCommand}"
                    Content="{Binding L[btn.delete]}" Padding="8,4"/>
			<Separator/>
			<Button Command="{Binding OpenSettingsCommand}"
                    Content="{Binding L[btn.settings]}" Padding="8,4"/>
		</ToolBar>

		<DataGrid Grid.Row="1"
                  ItemsSource="{Binding Jobs}"
                  SelectedItem="{Binding SelectedJob}"
                  AutoGenerateColumns="False"
                  IsReadOnly="True"
                  SelectionMode="Single"
                  GridLinesVisibility="Horizontal"
                  HeadersVisibility="Column"
                  Background="White">
			<DataGrid.Columns>
				<DataGridTextColumn Header="Name" Binding="{Binding Name}" Width="150"/>

				<DataGridTextColumn Header="Source" Binding="{Binding SourcePath}" Width="*"/>

				<DataGridTextColumn Header="Target" Binding="{Binding TargetPath}" Width="*"/>

				<DataGridTemplateColumn Header="Status" Width="100">
					<DataGridTemplateColumn.CellTemplate>
						<DataTemplate>
							<TextBlock Text="{Binding Status}" VerticalAlignment="Center" HorizontalAlignment="Center"/>
						</DataTemplate>
					</DataGridTemplateColumn.CellTemplate>
				</DataGridTemplateColumn>

				<DataGridTemplateColumn Header="Progress" Width="120">
					<DataGridTemplateColumn.CellTemplate>
						<DataTemplate>
							<Grid Margin="4,2">
								<ProgressBar Value="{Binding ProgressPercent}" Maximum="100" Height="14"/>
								<TextBlock Text="{Binding ProgressPercent, StringFormat={}{0}%}"
                                           FontSize="10" HorizontalAlignment="Center" VerticalAlignment="Center"/>
							</Grid>
						</DataTemplate>
					</DataGridTemplateColumn.CellTemplate>
				</DataGridTemplateColumn>

				<DataGridTemplateColumn Width="50">
					<DataGridTemplateColumn.CellTemplate>
						<DataTemplate>
							<Button Command="{Binding DataContext.RunJobCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                    CommandParameter="{Binding}"
                                    Content="▶"
                                    Padding="4,2"
                                    Background="Transparent"
                                    BorderThickness="0"
                                    Cursor="Hand"/>
						</DataTemplate>
					</DataGridTemplateColumn.CellTemplate>
				</DataGridTemplateColumn>
			</DataGrid.Columns>
		</DataGrid>

		<StatusBar Grid.Row="2" Margin="0,8,0,0" Background="Transparent">
			<StatusBarItem>
				<TextBlock Text="{Binding StatusMessage}" Foreground="Gray"/>
			</StatusBarItem>
			<StatusBarItem HorizontalAlignment="Right">
				<StackPanel Orientation="Horizontal">
					<TextBlock Text="Working..." Margin="0,0,8,0"
                               Visibility="{Binding IsBusy, Converter={StaticResource BoolToVis}}"
                               VerticalAlignment="Center" FontSize="11"/>
					<ProgressBar Width="100" Height="10" IsIndeterminate="True"
                                 Visibility="{Binding IsBusy, Converter={StaticResource BoolToVis}}"/>
				</StackPanel>
			</StatusBarItem>
		</StatusBar>
	</Grid>
</Window>